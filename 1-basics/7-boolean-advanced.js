// Challenge area

let temp = 200

// It is freezing outside! 31
// It is host outside! 110
// Go for it.  It is pretty nice. 45

if (temp >= 110) { console.log('It is hot outside!')
} else if (temp <= 31) { console.log('It is freezing outside!')
} else { console.log('Go for it.  It is pretty nice!')
}