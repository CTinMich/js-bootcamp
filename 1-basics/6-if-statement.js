// === - equality operator
// !== - not equal operator
// < - less than operator
// > - greater than operator
// <= - less than or equal to operator
// >= - greator then or equal to operator

let temp = 32
let isFreezing = temp <= 32

if (isFreezing) {
  console.log('It is freezing outside!')
}

// Challenge area

// Create age set to your age
let age = 64

// If 7 or under preint message about child pricing
if (age <= 7) {console.log('You recieved the child pricing.')
}

//if 65 or older print message about senior discount
if (age >= 65) {console.log('You recieved the senior discount.')
}